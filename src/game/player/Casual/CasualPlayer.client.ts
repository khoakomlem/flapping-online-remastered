import {type TickData} from '@/types/TickData';
import {PlayerClient} from '../Player/Player.client';

export class CasualPlayerClient extends PlayerClient {
	nextTick(_tickData: TickData): void {

	}
}
